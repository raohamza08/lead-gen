# dependencies
node_modules/
.pnpm-store/
__pycache__/
*.pyc
.venv/
venv/

# build output
dist/
build/
.next/
out/

# env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# editor/OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# prisma
apps/api/prisma/dev.db

# deploy tooling (local project linkage + pulled env, both machine-specific)
.vercel/

# misc
coverage/
.turbo/

# Appended by `vercel link`. `.env*` is kept but must be followed by the
# negation again — a later broader pattern overrides the earlier `!.env.example`
# further up this file, which would otherwise stop the template being tracked.
.vercel
.env*
!.env.example
