export default function AnalyticsPage() {
  return (
    <p className="text-sm text-ink/60">
      Full email/LinkedIn funnels, cohort trends, and revenue pipeline (Part F1). The Overview page&apos;s KPI strip and
      funnel chart are the starting point — extend <code>AnalyticsService</code> in{" "}
      <code>apps/api/src/analytics/analytics.service.ts</code> with the email-performance queries from Part E2.
    </p>
  );
}
